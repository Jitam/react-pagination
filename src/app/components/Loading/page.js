import React from 'react'

const Loading = () => {
    return (
        <div className='bg-black w-full top-0 left-0 h-screen fixed z-100 flex items-center justify-center text-white text-[2xl] font-semibold'>Loading...</div>
    )
}

export default Loading